// components/AdBanner.tsx
import React from 'react';
import { View, StyleSheet } from 'react-native';

// --- AdMob පරීක්ෂණය සඳහා සියලුම දේ තාවකාලිකව අක්‍රිය කර ඇත ---
// කිසිම AdMob import එකක් මෙහි නොමැත.

const AdBanner = () => {
  // දැන්වීම තාවකාලිකව අක්‍රිය කර ඇති නිසා, කිසිවක් නොපෙන්වමු.
  return null;
};

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: 'white',
    paddingVertical: 5,
  },
});

export default AdBanner;